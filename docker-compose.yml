version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: markdown_mongo
    ports:
      - "27017:27017"
    restart: unless-stopped

  redis:
    image: redis:alpine
    container_name: markdown_redis
    ports:
      - "6379:6379"
    restart: unless-stopped